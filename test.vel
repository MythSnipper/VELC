
string vel = "I love vel\n";
uint64 msglen = 11;

uint64 main() {
    char* pstr = (char*)"hello";



    return 0;
}



















void putchar(char out) {
    %{
        mov rax, 1          ; syscall: write
        mov rdi, 1          ; file descriptor: stdout
        ; pointer to message in rsi
        mov rdx, 1          ; message length
        syscall
    }$ in(out -> rsi)%
}
void puts(string str, uint64 strlen) {
    %{
        mov rax, 1          ; syscall: write
        mov rdi, 1          ; file descriptor: stdout
        ; pointer to message in rsi
        ; message length in rdx
        syscall
    }$ in(str -> rsi, strlen -> rdx)%
}






