#include:stdlib.vel

uint64 main() {

    for(int32 i = 0; i < 20; i++) {
        string t = "        ";
        sys_read(0, &t, 8);
        print_str(t);
        println("");
    }

    return 0;
}















